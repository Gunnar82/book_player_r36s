diff --git a/config.h b/config.h
index 26fb045..3b81d7e 100644
--- a/config.h
+++ b/config.h
@@ -1,7 +1,7 @@
 #ifndef CONFIG_H
 #define CONFIG_H
 #define APP_NAME "Hoerspiel Player"
-#define APP_VERSION "0.1.14"
+#define APP_VERSION "0.2.0-dev1"
 #define SCREEN_W 640
 #define SCREEN_H 480
 #define MAX_BOOKS 200
diff --git a/main.c b/main.c
index e1702f2..3c6f5ee 100644
--- a/main.c
+++ b/main.c
@@ -29,6 +29,8 @@
 #include "screens/sleeptimer.h"
 #include "screens/systeminfo.h"
 #include "screens/buttondebug.h"
+#include "screens/systemmenu.h"
+#include "screens/downloadbrowser.h"
 
 /*
  * Fuer die Tastensperre: Kandidaten-Buttons + deren Anzeigenamen.
@@ -96,6 +98,7 @@ int main(int argc, char **argv)
     setup_state_path();
     load_state();
     load_playback_config();
+    load_download_config();
 
     if (SDL_Init(SDL_INIT_VIDEO | SDL_INIT_AUDIO | SDL_INIT_JOYSTICK) != 0) {
         fprintf(stderr, "SDL_Init: %s\n", SDL_GetError());
@@ -409,7 +412,7 @@ int main(int argc, char **argv)
 
             /* Controller-Buttons, die auf jedem Bildschirm gelten. */
             if (e.type == SDL_JOYBUTTONDOWN) {
-                if (e.jbutton.button == BUTTON_X) { toggle_display(); continue; }
+                if (e.jbutton.button == BUTTON_X) { screen = SCREEN_SYSTEM_MENU; continue; }
 
                 if (e.jbutton.button == BUTTON_START) {
                     /* Start: global Play/Pause, sofern ein Track aktiv ist. */
@@ -465,6 +468,8 @@ int main(int argc, char **argv)
                     case SCREEN_SLEEP_TIMER: sleeptimer_handle_event(&screen_ctx, &e); break;
                     case SCREEN_SYSTEM_INFO: systeminfo_handle_event(&screen_ctx, &e); break;
                     case SCREEN_BUTTON_DEBUG: buttondebug_handle_event(&screen_ctx, &e); break;
+                    case SCREEN_SYSTEM_MENU: systemmenu_handle_event(&screen_ctx, &e); break;
+                    case SCREEN_DOWNLOADS: downloadbrowser_handle_event(&screen_ctx, &e); break;
                 }
         }
         }
@@ -784,6 +789,8 @@ if (action == MEDIA_KEY_STOP) {
             case SCREEN_SLEEP_TIMER: sleeptimer_render(&render_ctx); break;
             case SCREEN_SYSTEM_INFO: systeminfo_render(&render_ctx); break;
             case SCREEN_BUTTON_DEBUG: buttondebug_render(&render_ctx); break;
+            case SCREEN_SYSTEM_MENU: systemmenu_render(&render_ctx); break;
+            case SCREEN_DOWNLOADS: downloadbrowser_render(&render_ctx); break;
         }
 
         /* ---------------- Status oben rechts ---------------- */
diff --git a/screens.h b/screens.h
index e209891..65a93b7 100644
--- a/screens.h
+++ b/screens.h
@@ -12,7 +12,9 @@ typedef enum {
     SCREEN_PLAYER = 2,
     SCREEN_SLEEP_TIMER = 3,
     SCREEN_SYSTEM_INFO = 4,
-    SCREEN_BUTTON_DEBUG = 5
+    SCREEN_BUTTON_DEBUG = 5,
+    SCREEN_SYSTEM_MENU = 6,
+    SCREEN_DOWNLOADS = 7
 } ScreenId;
 
 #endif
