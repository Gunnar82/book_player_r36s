diff --git a/screens/systeminfo.c b/screens/systeminfo.c
index 161e68d..5b4164b 100644
--- a/screens/systeminfo.c
+++ b/screens/systeminfo.c
@@ -104,6 +104,8 @@ static int build_lines(ScreenContext *c,InfoLine *lines){
 
     /* Zuerst alles, was der Benutzer direkt aendern kann. */
     add_line(lines,&n,"AUDIO / DISPLAY","",1);
+    snprintf(buf,sizeof(buf),"<  %s  >",downloads_enabled?"An":"Aus");
+    add_line(lines,&n,"Downloads",buf,0);
     snprintf(buf,sizeof(buf),"<  %d %%  >",(volume*100)/MIX_MAX_VOLUME);
     add_line(lines,&n,"Lautstaerke",buf,0);
 
@@ -202,6 +204,7 @@ static int build_lines(ScreenContext *c,InfoLine *lines){
 
     return n;
 }
+static int find_line(InfoLine *lines,int count,const char *label){for(int i=0;i<count;i++)if(!strcmp(lines[i].label,label))return i;return -1;}
 static int find_led_test_line(InfoLine *lines,int count){for(int i=0;i<count;i++)if(!strcmp(lines[i].label,"LED Test"))return i;return -1;}
 static int find_repeat_line(InfoLine *lines,int count){for(int i=0;i<count;i++)if(!strcmp(lines[i].label,"Hoerspielende"))return i;return -1;}
 static int find_shutdown_tracks_line(InfoLine *lines,int count){for(int i=0;i<count;i++)if(!strcmp(lines[i].label,"Shutdown nach Tracks"))return i;return -1;}
@@ -213,6 +216,7 @@ static int find_sleep_line(InfoLine *lines,int count){for(int i=0;i<count;i++)if
 static int find_idle_line(InfoLine *lines,int count){for(int i=0;i<count;i++)if(!strcmp(lines[i].label,"Idle-Timer"))return i;return -1;}
 static int next_selectable(InfoLine *lines,int count,int from,int dir){int i=from+dir;while(i>=0&&i<count){if(!lines[i].heading)return i;i+=dir;}return from;}
 static void keep_visible(int count){int max=count-MAX_VISIBLE;if(max<0)max=0;if(selected_line<page_offset)page_offset=selected_line;if(selected_line>=page_offset+MAX_VISIBLE)page_offset=selected_line-MAX_VISIBLE+1;if(page_offset<0)page_offset=0;if(page_offset>max)page_offset=max;}
+static void toggle_downloads(void){downloads_enabled=!downloads_enabled;if(save_download_enabled()!=0)fprintf(stderr,"Einstellungen: Download-Status konnte nicht gespeichert werden.\n");}
 static void change_led_gpio(int delta){
     int gpio=led_get_gpio();
     if(gpio<0) gpio=0;
@@ -280,6 +284,8 @@ void systeminfo_handle_event(ScreenContext *c,const SDL_Event *e){
         int rl=find_repeat_line(lines,count);
         int stl=find_shutdown_tracks_line(lines,count);
         int sel=find_shutdown_end_line(lines,count);
+        int dl=find_line(lines,count,"Downloads");
+        if((b==BUTTON_A||b==BUTTON_DPAD_LEFT||b==BUTTON_DPAD_RIGHT)&&selected_line==dl){toggle_downloads();return;}
         if((b==BUTTON_A||b==BUTTON_DPAD_LEFT||b==BUTTON_DPAD_RIGHT)&&selected_line==rl){toggle_repeat_book();return;}
         if(b==BUTTON_DPAD_LEFT&&selected_line==stl){change_shutdown_tracks(-1);return;}
         if(b==BUTTON_DPAD_RIGHT&&selected_line==stl){change_shutdown_tracks(1);return;}
@@ -302,8 +308,8 @@ void systeminfo_handle_event(ScreenContext *c,const SDL_Event *e){
     if(e->type==SDL_JOYAXISMOTION){
         if(e->jaxis.axis==AXIS_Y){if(!*c->axis_y_lock&&e->jaxis.value<-AXIS_DEADZONE){selected_line=next_selectable(lines,count,selected_line,-1);keep_visible(count);*c->axis_y_lock=1;}else if(!*c->axis_y_lock&&e->jaxis.value>AXIS_DEADZONE){selected_line=next_selectable(lines,count,selected_line,1);keep_visible(count);*c->axis_y_lock=1;}if(abs(e->jaxis.value)<AXIS_DEADZONE)*c->axis_y_lock=0;}
         if(e->jaxis.axis==AXIS_X){
-            int gl=find_led_gpio_line(lines,count), tl=find_led_test_line(lines,count), vl=find_volume_line(lines,count), bl=find_brightness_line(lines,count), sl=find_sleep_line(lines,count), il=find_idle_line(lines,count), rl=find_repeat_line(lines,count), stl=find_shutdown_tracks_line(lines,count), sel=find_shutdown_end_line(lines,count);
-            if(selected_line==rl){if(!*c->axis_x_lock&&abs(e->jaxis.value)>AXIS_DEADZONE){toggle_repeat_book();*c->axis_x_lock=1;}}
+            int gl=find_led_gpio_line(lines,count), tl=find_led_test_line(lines,count), vl=find_volume_line(lines,count), bl=find_brightness_line(lines,count), sl=find_sleep_line(lines,count), il=find_idle_line(lines,count), rl=find_repeat_line(lines,count), stl=find_shutdown_tracks_line(lines,count), sel=find_shutdown_end_line(lines,count), dl=find_line(lines,count,"Downloads");
+            if(selected_line==dl){if(!*c->axis_x_lock&&abs(e->jaxis.value)>AXIS_DEADZONE){toggle_downloads();*c->axis_x_lock=1;}}else if(selected_line==rl){if(!*c->axis_x_lock&&abs(e->jaxis.value)>AXIS_DEADZONE){toggle_repeat_book();*c->axis_x_lock=1;}}
             else if(selected_line==stl){if(!*c->axis_x_lock&&e->jaxis.value<-AXIS_DEADZONE){change_shutdown_tracks(-1);*c->axis_x_lock=1;}else if(!*c->axis_x_lock&&e->jaxis.value>AXIS_DEADZONE){change_shutdown_tracks(1);*c->axis_x_lock=1;}}
             else if(selected_line==sel){if(!*c->axis_x_lock&&abs(e->jaxis.value)>AXIS_DEADZONE){toggle_shutdown_end();*c->axis_x_lock=1;}}
             else if(selected_line==tl){if(!*c->axis_x_lock&&abs(e->jaxis.value)>AXIS_DEADZONE){led_set_test_mode(!led_test_mode());*c->axis_x_lock=1;}}
diff --git a/screens/systemmenu.c b/screens/systemmenu.c
index 09ba905..55d9ba9 100644
--- a/screens/systemmenu.c
+++ b/screens/systemmenu.c
@@ -1,10 +1,14 @@
 #include "systemmenu.h"
 #include "../ui.h"
+#include "../storage.h"
+#include "downloadbrowser.h"
 #include <stdlib.h>
+#include <stdio.h>
 
 static int selection = 0;
 static const char *items[] = {
     "Einstellungen",
+    "Downloads",
     "Button Debug",
     "Beenden",
     "Herunterfahren"
@@ -22,9 +26,15 @@ static void activate(ScreenContext *c)
 {
     switch (selection) {
         case 0: *c->screen = SCREEN_SYSTEM_INFO; break;
-        case 1: *c->screen = SCREEN_BUTTON_DEBUG; break;
-        case 2: *c->running = 0; break;
-        case 3:
+        case 1:
+            if (downloads_enabled) {
+                downloadbrowser_reset();
+                *c->screen = SCREEN_DOWNLOADS;
+            }
+            break;
+        case 2: *c->screen = SCREEN_BUTTON_DEBUG; break;
+        case 3: *c->running = 0; break;
+        case 4:
             c->shutdown_fn(c->music);
             *c->running = 0;
             break;
@@ -60,8 +70,12 @@ void systemmenu_render(ScreenContext *c)
 
     int y = 90;
     for (int i = 0; i < ITEM_COUNT; i++) {
-        SDL_Color col = (i == selection) ? c->selected : c->white;
-        draw_text(c->renderer, c->font, items[i], 45, y, col);
+        int disabled = (i == 1 && !downloads_enabled);
+        SDL_Color col = disabled ? c->gray : ((i == selection) ? c->selected : c->white);
+        char label[128];
+        if (disabled) snprintf(label,sizeof(label),"%s  [deaktiviert]",items[i]);
+        else snprintf(label,sizeof(label),"%s",items[i]);
+        draw_text(c->renderer, c->font, label, 45, y, col);
         y += 42;
     }
 
