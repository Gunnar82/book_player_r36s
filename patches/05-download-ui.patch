diff --git a/screens/downloadbrowser.c b/screens/downloadbrowser.c
new file mode 100644
index 0000000..6ca5949
--- /dev/null
+++ b/screens/downloadbrowser.c
@@ -0,0 +1,172 @@
+#include "downloadbrowser.h"
+#include "../download.h"
+#include "../storage.h"
+#include "../ui.h"
+
+#include <SDL2/SDL.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+
+#define ROW_H 30
+#define TOP_Y 72
+#define BOTTOM_Y 410
+#define MAX_ROWS ((BOTTOM_Y-TOP_Y)/ROW_H)
+
+static RemoteEntry entries[REMOTE_MAX_ENTRIES];
+static int entry_count=0;
+static int selection=0;
+static int scroll_offset=0;
+static int loaded=0;
+static int loading=0;
+static char relative_path[2048]="";
+static char status[256]="";
+static int finished_download=0;
+
+static int files_in_listing(void){int n=0;for(int i=0;i<entry_count;i++)if(entries[i].type==REMOTE_FILE)n++;return n;}
+static int virtual_count(void){return entry_count + (files_in_listing()>0?1:0);}
+static void keep_visible(void){int rows=MAX_ROWS;if(selection<scroll_offset)scroll_offset=selection;if(selection>=scroll_offset+rows)scroll_offset=selection-rows+1;int max=virtual_count()-rows;if(max<0)max=0;if(scroll_offset<0)scroll_offset=0;if(scroll_offset>max)scroll_offset=max;}
+static void load_current(void){loading=1;status[0]='\0';entry_count=remote_fetch_listing(relative_path,entries,REMOTE_MAX_ENTRIES,status,sizeof(status));if(entry_count<0)entry_count=0;selection=0;scroll_offset=0;loaded=1;loading=0;finished_download=0;}
+static void parent_dir(void){if(!relative_path[0])return;char *slash=strrchr(relative_path,'/');if(slash)*slash='\0';else relative_path[0]='\0';loaded=0;}
+static int is_download_action_index(int idx){return files_in_listing()>0 && idx==0;}
+static int entry_index_from_virtual(int idx){return idx-(files_in_listing()>0?1:0);}
+static void enter_directory(const char *name){if(relative_path[0])strncat(relative_path,"/",sizeof(relative_path)-strlen(relative_path)-1);strncat(relative_path,name,sizeof(relative_path)-strlen(relative_path)-1);loaded=0;}
+static int progress_render(const char *name,int file_index,int file_count,long long file_now,long long file_total,long long total_now,long long total_size,void *userdata){
+    ScreenContext *c=(ScreenContext*)userdata;SDL_Event e;
+    while(SDL_PollEvent(&e)){if(e.type==SDL_QUIT)return 1;if(e.type==SDL_JOYBUTTONDOWN && e.jbutton.button==BUTTON_B)return 1;}
+    SDL_SetRenderDrawColor(c->renderer,20,20,30,255);SDL_RenderClear(c->renderer);draw_text(c->renderer,c->font,"Download",20,20,c->selected);
+    char line[256];snprintf(line,sizeof(line),"%d / %d  %s",file_index,file_count,name?name:"");draw_text(c->renderer,c->font,line,20,90,c->white);
+    if(file_total>0){snprintf(line,sizeof(line),"Datei: %.1f / %.1f MB",file_now/1048576.0,file_total/1048576.0);draw_text(c->renderer,c->font,line,20,135,c->white);}
+    if(total_size>0){double pct=100.0*(double)total_now/(double)total_size;if(pct>100)pct=100;snprintf(line,sizeof(line),"Gesamt: %.1f / %.1f MB  (%.0f %%)",total_now/1048576.0,total_size/1048576.0,pct);draw_text(c->renderer,c->font,line,20,180,c->white);SDL_Rect bar={20,225,580,10};SDL_SetRenderDrawColor(c->renderer,70,70,70,255);SDL_RenderFillRect(c->renderer,&bar);SDL_Rect fill=bar;fill.w=(int)(bar.w*pct/100.0);SDL_SetRenderDrawColor(c->renderer,230,210,70,255);SDL_RenderFillRect(c->renderer,&fill);}
+    draw_text(c->renderer,c->font,"B: Abbrechen",20,390,c->gray);SDL_RenderPresent(c->renderer);return 0;
+}
+static void start_download(ScreenContext *c){char err[256]="";int rc=remote_download_files(relative_path,entries,entry_count,progress_render,c,err,sizeof(err));if(rc>0){snprintf(status,sizeof(status),"%d Dateien geladen. Neustart zum Einlesen.",rc);finished_download=1;}else snprintf(status,sizeof(status),"%s",err[0]?err:"Download fehlgeschlagen");}
+void downloadbrowser_reset(void){relative_path[0]='\0';entry_count=0;selection=0;scroll_offset=0;loaded=0;status[0]='\0';finished_download=0;}
+void downloadbrowser_handle_event(ScreenContext *c,const SDL_Event *e){
+    if(!loaded&&!loading)load_current();
+    if(e->type==SDL_JOYBUTTONDOWN){int b=e->jbutton.button;if(b==BUTTON_B){if(relative_path[0]){parent_dir();return;}*c->screen=SCREEN_SYSTEM_MENU;return;}int vc=virtual_count();if(b==BUTTON_DPAD_UP){selection--;if(selection<0)selection=vc>0?vc-1:0;keep_visible();return;}if(b==BUTTON_DPAD_DOWN){selection++;if(selection>=vc)selection=0;keep_visible();return;}if(b==BUTTON_L1){selection-=LIST_PAGE_SIZE;if(selection<0)selection=0;keep_visible();return;}if(b==BUTTON_R1){selection+=LIST_PAGE_SIZE;if(selection>=vc)selection=vc>0?vc-1:0;keep_visible();return;}if(b==BUTTON_L2){selection=0;keep_visible();return;}if(b==BUTTON_R2){selection=vc>0?vc-1:0;keep_visible();return;}if(b==BUTTON_A&&vc>0){if(is_download_action_index(selection)){start_download(c);return;}int ei=entry_index_from_virtual(selection);if(ei>=0&&ei<entry_count&&entries[ei].type==REMOTE_DIRECTORY){enter_directory(entries[ei].name);return;}}}
+    if(e->type==SDL_JOYAXISMOTION&&e->jaxis.axis==AXIS_Y){int vc=virtual_count();if(!*c->axis_y_lock&&e->jaxis.value<-AXIS_DEADZONE){selection--;if(selection<0)selection=vc>0?vc-1:0;keep_visible();*c->axis_y_lock=1;}else if(!*c->axis_y_lock&&e->jaxis.value>AXIS_DEADZONE){selection++;if(selection>=vc)selection=0;keep_visible();*c->axis_y_lock=1;}if(abs(e->jaxis.value)<AXIS_DEADZONE)*c->axis_y_lock=0;}
+}
+void downloadbrowser_render(ScreenContext *c){
+    if(!loaded&&!loading)load_current();draw_text(c->renderer,c->font,"Downloads",20,20,c->selected);char pathline[2200];if(relative_path[0])snprintf(pathline,sizeof(pathline),"/%s",relative_path);else snprintf(pathline,sizeof(pathline),"/");draw_text(c->renderer,c->font,pathline,20,48,c->gray);
+    if(status[0])draw_text(c->renderer,c->font,status,20,SCREEN_H-58,finished_download?c->selected:c->gray);
+    if(entry_count==0){if(!status[0])draw_text(c->renderer,c->font,"Kein Listing verfuegbar",20,100,c->gray);draw_text(c->renderer,c->font,"B: Zurueck",20,SCREEN_H-25,c->gray);return;}
+    int vc=virtual_count();int end=scroll_offset+MAX_ROWS;if(end>vc)end=vc;int y=TOP_Y;
+    for(int vi=scroll_offset;vi<end;vi++,y+=ROW_H){SDL_Color col=vi==selection?c->selected:c->white;if(is_download_action_index(vi)){char b[128];snprintf(b,sizeof(b),"[Ordner herunterladen: %d Dateien]",files_in_listing());draw_text(c->renderer,c->font,b,32,y,col);continue;}int ei=entry_index_from_virtual(vi);if(ei<0||ei>=entry_count)continue;char label[620];if(entries[ei].type==REMOTE_DIRECTORY)snprintf(label,sizeof(label),"%s/",entries[ei].name);else if(entries[ei].size>0)snprintf(label,sizeof(label),"%s  (%.1f MB)",entries[ei].name,entries[ei].size/1048576.0);else snprintf(label,sizeof(label),"%s",entries[ei].name);draw_text(c->renderer,c->font,label,32,y,col);}
+    draw_text(c->renderer,c->font,"A: Oeffnen/Download   B: Zurueck",20,SCREEN_H-25,c->gray);
+}
diff --git a/screens/downloadbrowser.h b/screens/downloadbrowser.h
new file mode 100644
index 0000000..0ad6e4a
--- /dev/null
+++ b/screens/downloadbrowser.h
@@ -0,0 +1,7 @@
+#ifndef SCREEN_DOWNLOAD_BROWSER_H
+#define SCREEN_DOWNLOAD_BROWSER_H
+#include "screen_context.h"
+void downloadbrowser_handle_event(ScreenContext *ctx,const SDL_Event *e);
+void downloadbrowser_render(ScreenContext *ctx);
+void downloadbrowser_reset(void);
+#endif
